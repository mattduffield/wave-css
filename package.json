{
  "name": "wave-css",
  "version": "0.0.1",
  "main": "esbuild.config.js",
  "scripts": {
    "build": "node esbuild.config.js && npm run build:icons",
    "build:icons": "node scripts/bundle-icons.js",
    "release": "npm run build && node scripts/release.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Matt Duffield",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
